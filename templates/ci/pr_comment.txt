{% macro test_list(tests) -%}
<ul>
{% for test in tests %}
<li> ccextractor {{ test.command }} <a href="{{ url_for('sample.sample_by_id', sample_id=test.sample.id, _external=True) }}">{{ test.sample.sha[:10] }}...</a></li>
{% endfor %}
</ul>
{%- endmacro %}
{% set changed = namespace(here=[], reference=none) %}
{% for comparison in comment_info.comparisons %}
{% if comparison.run and not comparison.duplicate_of and changed.reference is none %}
{% set changed.here = comparison.tests.broken_here + comparison.tests.failing_differently %}
{% set changed.reference = comparison %}
{% endif %}
{% endfor %}
<details>
<summary><b>CCExtractor CI platform</b> finished running the test files on <b>{{platform}}</b>. {{ comment_info.passed_count }}/{{ comment_info.total_count }} tests matched the approved output:</summary>
<table>
<thead>
<td> Report Name </td>
<td> Tests Passed </td>
</thead>
{% for test in comment_info.category_stats %}
<tr>
<td> {{test.category}} </td>
{% if test.success==None %}
<td> <b> 0/{{test.total}}  </b> </td>
{% elif test.success == test.total %}
<td> {{test.success}}/{{test.total}} </td>
{% else %}
<td> <b> {{test.success}}/{{test.total}} </b> </td>
{% endif %}
</tr>
{% endfor %}
</table>

{% if comment_info.failed_tests | length %}
<b>{{ comment_info.failed_tests | length }} tests do not match the approved output.</b> That is the pass/fail verdict. Whether this branch caused it is a separate question, answered below.
{{ test_list(comment_info.failed_tests) }}
{% endif %}

{% for comparison in comment_info.comparisons %}
<hr>
{% if comparison.duplicate_of %}
<b>Compared with {{ comparison.label }}:</b> the same run as {{ comparison.duplicate_of }} (test {{ comparison.run.id }}), so the comparison above already covers it.
{% elif not comparison.run %}
<b>Compared with {{ comparison.label }}:</b> no completed run found, so nothing can be said about this reference. This is not the same as agreement.
{% else %}
<b>Compared with {{ comparison.label }}</b> — <a href="{{ url_for('test.by_id', test_id=comparison.run.id, _external=True) }}">test {{ comparison.run.id }}</a>, commit {{ comparison.run.commit[:7] }}:
<ul>
<li><b>{{ comparison.counts.broken_here }}</b> pass there and fail here</li>
<li><b>{{ comparison.counts.fixed_here }}</b> fail there and pass here</li>
<li><b>{{ comparison.counts.failing_differently }}</b> fail on both, with different output</li>
<li><b>{{ comparison.counts.failing_identically }}</b> fail on both, byte for byte the same</li>
{% if comparison.counts.no_reference %}
<li><b>{{ comparison.counts.no_reference }}</b> have no result in that run</li>
{% endif %}
</ul>
{% if comparison.tests.broken_here | length %}
Pass there, fail here:
{{ test_list(comparison.tests.broken_here) }}
{% endif %}
{% if comparison.tests.failing_differently | length %}
Fail on both but produce different output — behaviour moved even though the verdict did not:
{{ test_list(comparison.tests.failing_differently) }}
{% endif %}
{% if comparison.tests.fixed_here | length %}
Fail there, pass here:
{{ test_list(comparison.tests.fixed_here) }}
{% endif %}
{% endif %}
{% endfor %}
<br>
</details>

{% if changed.reference is none %}
{% if comment_info.failed_tests | length %}
{{ comment_info.failed_tests | length }} tests do not match the approved output. There is no completed run to compare against, so this report cannot say whether this branch caused them — treat the list above as unattributed.
{% else %}
All tests passed: every output matched the approved file.
{% endif %}
{% elif changed.here | length %}
This branch changes the behaviour of {{ changed.here | length }} test(s) relative to {{ changed.reference.label }}. Those are the ones worth looking at; anything else in the list fails the same way on both sides.
{% elif comment_info.failed_tests | length %}
No test changes behaviour relative to {{ changed.reference.label }}: every failure above fails there too, byte for byte. The approved output for those tests is out of date, which is a baseline to review rather than a regression in this branch.
{% else %}
All tests passed: every output matched the approved file.
{% endif %}
